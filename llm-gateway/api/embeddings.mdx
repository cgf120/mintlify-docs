---
title: "Embeddings"
api: "POST https://api.chenyu.cn/v1/embeddings"
description: "将文本转换为向量"
authMethod: "bearer"
---

# Embeddings

将文本输入转换为向量，用于检索、聚类、相似度计算等场景。

## 请求参数

<ParamField body="model" type="string" required>
  Embedding 模型 ID
</ParamField>

<ParamField body="input" type="string | array" required>
  输入文本，可以是字符串或字符串数组
</ParamField>

## 响应参数

<ResponseField name="object" type="string">
  固定为 `list`
</ResponseField>

<ResponseField name="data" type="array">
  向量列表

  <Expandable title="data">
    <ResponseField name="object" type="string">
      固定为 `embedding`
    </ResponseField>

    <ResponseField name="index" type="integer">
      输入序号
    </ResponseField>

    <ResponseField name="embedding" type="array">
      向量数组
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="usage" type="object">
  token 用量
</ResponseField>

## 代码示例

<CodeGroup>

```python Python
import requests

url = "https://api.chenyu.cn/v1/embeddings"
headers = {
    "Authorization": "Bearer your_api_key",
    "Content-Type": "application/json"
}
payload = {
    "model": "doubao-embedding-large-text-250515",
    "input": ["第一段文本", "第二段文本"]
}

response = requests.post(url, headers=headers, json=payload)
print(response.json())
```

```javascript JavaScript
const axios = require('axios');

axios.post('https://api.chenyu.cn/v1/embeddings', {
  model: 'doubao-embedding-large-text-250515',
  input: ['第一段文本', '第二段文本']
}, {
  headers: {
    Authorization: 'Bearer your_api_key',
    'Content-Type': 'application/json'
  }
}).then((response) => {
  console.log(response.data);
});
```

```curl cURL
curl -X POST "https://api.chenyu.cn/v1/embeddings" \
  -H "Authorization: Bearer your_api_key" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "doubao-embedding-large-text-250515",
    "input": ["第一段文本", "第二段文本"]
  }'
```

</CodeGroup>

## 响应示例

```json
{
  "object": "list",
  "data": [
    {
      "object": "embedding",
      "index": 0,
      "embedding": [0.0123, -0.0456, 0.0789]
    }
  ],
  "model": "doubao-embedding-large-text-250515",
  "usage": {
    "prompt_tokens": 8,
    "total_tokens": 8
  }
}
```
